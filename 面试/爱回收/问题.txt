爱回收一面
1)写组件时构造函数中为什么有super(props)
2)数组有哪些方法，foreach和map区别，concat是否返回新数组

参考：
1)
super关键字，它指代父类的实例（即父类的this对象）。子类必须在constructor方法中调用super方法，否则新建实例时会报错。
这是因为子类没有自己的this对象，而是继承父类的this对象，然后对其进行加工。如果不调用super方法，子类就得不到this对象。

2）foreach不会返回新数组，而map则会返回新数组，原数组不更改
    concat同样不会更改原数组，返回新数组


二面
1)类继承，写原型
2)orm如何转换
3)purecomponent
4)setState介绍

参考：
1）见class.js



