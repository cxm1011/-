1.为什么不用fetch，而使用axios
axios请求能取消
2.假设
<ul>
    <childLi />
</ul>
在childLi组件中该如何写？
<>
<li></li>
<li></li>
</>
3.直接操作dom不行吗？为什么要使用VDom?
如果使用循环依次操作dom,会不断重构页面，带来性能下降。而使用VDom，其只在最后一次才渲染dom

4.如何实现搜索框输入文字后会自动调后端接口？
使用setTimeOut定时函数，比如1s后调用回调函数，当在这1s内有输入的话，应清空定时器